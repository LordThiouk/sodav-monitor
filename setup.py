from setuptools import find_packages, setup

setup(
    name="sodav-monitor",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Les dépendances seront lues depuis requirements.txt
    ],
)
